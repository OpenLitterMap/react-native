v7.4.3 — Critical GPS sign bug fix: both camera metadata and EXIF gallery paths now apply Math.abs() before LatitudeRef/LongitudeRef sign, preventing double-negation that placed Western/Southern hemisphere photos in the wrong location (Ireland → Germany). Tag failure now blocks navigation during onboarding (prevents broken loop where server never sets onboarding_completed_at). Removed totalImages > 0 skip condition (backend backfill confirmed). Fixed uploadedUris crash on rehydrated state. Dismissed gallery photos now removed from imagesArray (fixes ghost upload button). CelebrationScreen reads lat/lon from route params instead of Redux (photo is spliced before celebration renders). TagDetailSheet wired into onboarding tag screen. Camera "not available" screen now has Go Back button. Tooltip flow reworked: dismiss hides without advancing, each step triggered by user action only. Updated celebration copy and HomeScreen labels.
