## 7.6.0 — 2026-04-04

- Quick Tags: custom names — users can rename presets via Settings > Quick Tags
- Quick Tags: new favourites appear at the top of the chip bar
- Quick Tags: suggestions/search apply preset metadata (materials, brands, picked_up) when a matching quick tag exists
- Quick Tags: re-tapping a preset merges materials/brands additively instead of ignoring them
- Quick Tags: description text added to settings screen
- Quick Tags: all strings translated across 8 languages
- Tiered max quantity: trusted users can set up to 100, new users capped at 10 (backend-driven via verification_required)
- Fix: "GO_BACK not handled" crash when closing tagging screen (double goBack race condition)
- Fix: star button in search results no longer also selects the tag (RN event bleeding)
- Fix: sync ping-pong loop — fulfilled handler no longer replaces presets with server response
- Fix: QuickTagEditModal customName useCallback dependency
- Fix: quickTagPresets selector declaration order in AddTagScreen
- Backend: custom_name column on user_quick_tags (VARCHAR 60, nullable)
- Backend: tiered quantity validation (1-100 trusted, 1-10 default)
