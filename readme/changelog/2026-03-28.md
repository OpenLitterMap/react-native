v7.3.3 — Fixed Apple rejection (Guideline 5.1.1iv): InboxSection now shows native iOS permission dialog for `denied` state instead of redirecting to Settings. Settings link only appears after user has actively denied permission (`blocked` state).
