- Fix: bottom tab bar overlap on Android 16 edge-to-edge (Galaxy S25 FE / One UI 8.0) — `routes/TabRoutes.tsx` now adds `useSafeAreaInsets().bottom` to the tab bar height and paddingBottom so the system gesture bar no longer covers tab icons/labels when `targetSdk` 36 forces edge-to-edge
- Security: bumped axios `^1.7.2` → `^1.15.0` to resolve two critical advisories (GHSA-fvcv-3m26-pcqx cloud metadata exfiltration, GHSA-3p68-rc4w-qgx5 NO_PROXY SSRF bypass); pinned `react-test-renderer` to exact `19.2.3` to match `react` and unblock the peer-dep resolve
- Fix: iPhone tab bar was too tall after the Android 16 safe-area fix (88 base + inset double-counted); reduced base to 56 so notched iPhones land at ~90px, SE/Android at 56
- UI: tab bar iconography polish — replaced emoji (🌍/🧗/📱) with Ionicons (`earth`, `trophy`, `person-circle`) that respect active/inactive tint colors, with filled/outline variants for clearer focus state
