# 2026-04-05 — v7.7.2

- Performance: consolidated selectors (CommunityStats uses selectStats, HomeScreen uses selectUploadFlow), hoisted SettingsScreen static data, memoized LeaderboardsTab/UntaggedSection/LimitedAccessBanner, extracted store-level selectTaggedUris + selectCameraPhotos selectors, added getItemLayout to InboxSection FlatList, removed TagPills IIFE
- Performance: YourImpactSection fetchXpLevels guarded with hasFetched ref (static config, 7-day cache)
- UX: TagDetailSheet swipe-down-to-close gesture (tracks finger, scroll-aware, springs back on incomplete swipe)
- Build: synced iOS (build 78) and Android (build 55) versions to match package.json
