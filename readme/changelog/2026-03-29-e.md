v7.4.4 — Codex review fixes: map/share links in UploadsPreview and MyUploads now use WEB_URL instead of API URL. Done button enabled when pending custom tag is typed (custom-tag-only flow unblocked). UploadsPreview loading state reads correct fetchStatus field. Native versions synced to 7.4.4 (Android versionCode 53, iOS MARKETING_VERSION). Renamed actions/types.js to utils/config.js, simplified env config (IS_PRODUCTION toggle selects hardcoded URL pairs). Fixed classifyError misclassifying "already uploaded" 422s as generic validation (now catches message field + safe string coercion). Silenced expected Sentry noise from EXIF timeouts and ACCESS_MEDIA_LOCATION exceptions.
