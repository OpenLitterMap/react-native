v7.4.0 — Onboarding now uploads photo + submits tags to the server before showing celebration (two-step: POST /api/v3/upload then POST /api/v3/tags). Celebration screen shows photo-specific geolink (View on the map + Copy link) using dynamic base URL from env. Added GPS camera setup instructions (iPhone/Android toggle) on welcome screen. 3-pass audit: 21 bug fixes across structural, deep code, and external review — including Android limited gallery GPS dead end, CameraCapture battery/double-tap/error guards, MainRoutes existing-user migration + race prevention, permission state machine (7 states), tag fetch retry, picked_up toggle, and full i18n (all 8 languages).
