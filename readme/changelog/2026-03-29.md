# 2026-03-29

## v7.4.0
Onboarding now uploads photo + submits tags to the server before showing celebration (two-step: POST /api/v3/upload then POST /api/v3/tags). Celebration screen shows photo-specific geolink using dynamic base URL from env. Added GPS camera setup instructions on welcome screen. 3-pass audit: 21 bug fixes across structural, deep code, and external review — including Android limited gallery GPS dead end, CameraCapture battery/double-tap/error guards, MainRoutes existing-user migration + race prevention, permission state machine (7 states), tag fetch retry, picked_up toggle, and full i18n (all 8 languages).

## v7.4.1
Fixed upload date field sent as ISO string instead of unix seconds (was silently dropped by backend). Added web onboarding sync: MainRoutes reads user.onboarding_completed_at from profile response to skip mobile onboarding for web-onboarded users. Welcome screen GPS instructions now auto-detect platform (removed toggle). Updated onboarding copy. Adjusted StepIndicator and welcome screen spacing.

## v7.4.2
Camera preview: fixed rotation/zoom (resizeMode=contain), added GPS lat/lon display + delete button, action buttons anchored to bottom. StepIndicator: light background on camera and tag screens, "Take photo" label for camera path. OnboardingTagScreen: delete button on photo, tooltip repositioned. CelebrationScreen: rewritten with "You did it!" copy, geolink with copy button, sharing note, council tip. Welcome screen: "Most Compatible" JPG step for iOS, privacy note. HomeScreen: camera-captured photos shown with camera badge, uploaded photos filtered out.

## v7.4.3
Critical GPS sign bug fix: both camera metadata and EXIF gallery paths now apply Math.abs() before LatitudeRef/LongitudeRef sign, preventing double-negation that placed Western/Southern hemisphere photos in the wrong location (Ireland appeared in Germany). Tag failure now blocks navigation during onboarding (prevents broken loop where server never sets onboarding_completed_at). Removed totalImages > 0 skip condition (backend backfill confirmed). Fixed uploadedUris crash on rehydrated state. Dismissed gallery photos now removed from imagesArray (fixes ghost upload button). CelebrationScreen reads lat/lon from route params instead of Redux. TagDetailSheet wired into onboarding tag screen. Camera "not available" screen has Go Back button. Tooltip flow reworked: dismiss hides without advancing, each step triggered by user action only.

## v7.4.4
Codex review fixes: map/share links in UploadsPreview and MyUploads now use WEB_URL instead of API URL. Done button enabled when pending custom tag is typed (custom-tag-only flow unblocked). UploadsPreview loading state reads correct fetchStatus field. Native versions synced (Android versionCode 53, iOS MARKETING_VERSION). Renamed actions/types.js to utils/config.js, simplified env config. Fixed classifyError misclassifying "already uploaded" 422s as generic validation. Silenced expected Sentry noise from EXIF timeouts and ACCESS_MEDIA_LOCATION exceptions.

## v7.5.0
Production release. Upload confirmation dialog: "Upload now?" with "Upload later" option (saves tags to queue, skips to HomeScreen). Settings text inputs fixed: white-on-white text resolved with variant="light" on all CustomTextInput instances + styled password field. NSAllowsArbitraryLoads set back to false for App Store compliance. GalleryPermissionScreen fixed: uses goBack() instead of navigate() to dismiss modal properly. Camera preview buttons: restructured layout with safe area padding, Delete/Retake in a row above full-width "Use this photo" primary button.
