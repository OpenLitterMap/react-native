v7.2.0 — HomeScreen dashboard redesign: replaced Instagram-style photo grid with 4-section scrollable dashboard (Global Impact, Your Impact, Uploaded/Untagged, Camera Roll Inbox). Eliminated 6-step gallery import flow in favor of 3-step tap-to-tag model. Added permission re-check on foreground/focus, "Show older photos" pagination, "Tag All" for both sections. Deleted GalleryScreen and dead home components.

v7.3.0 — Photo accessibility rearchitecture: replaced auto-upload with explicit Upload button, fixed queue contamination and token-expiry recovery, fixed stale untagged previews after tagging/deleting, added permission CTA for denied state, platform-specific limited-access banner (iOS vs Android), expandable recency window for "Show older photos", tags persist on back from AddTagScreen, inbox tap loads full photo set for swiping, replaced Tag All with Delete mode on inbox, moved community stats and XP level detail to HomeScreen, ProfileScreen redesign (inline uploads preview with Load More, settings button, logout in nav), TagChips fallback to summary when structured tags missing, photo ID shown on upload cards.

v7.3.1 — Tab bar UI redesign: switched from material-top-tabs to @react-navigation/bottom-tabs, white background with larger emojis, Poppins labels (Home, Leaderboards, Account), proper safe area handling. Changed limited-access banner from yellow warning to green encouragement ("Import photos to tag"). Renamed untagged section header to "Uploaded - Not Tagged".

v7.3.2 — Fixed createTeam payload: `team_type` → `teamType` (matching Laravel CreateTeamRequest validation). Hidden "Create" team button when `remaining_teams` is 0.
