v7.4.1 — Fixed upload date field sent as ISO string instead of unix seconds (was silently dropped by backend). Added web onboarding sync: MainRoutes reads user.onboarding_completed_at from profile response to skip mobile onboarding for web-onboarded users. Welcome screen GPS instructions now auto-detect platform (removed iPhone/Android toggle). Updated onboarding copy. Adjusted StepIndicator and welcome screen spacing.
