- Fix: tag-post 422 "The photo id field must be an integer" (~494 Sentry events / 11 users) — `addTagsToPhoto` (renamed from `postTagsToPhoto`) now guards `photo_id` with `utils/isServerPhotoId.js` so a local/onboarding placeholder id never reaches `/api/v3/tags`; the duplicate-upload `"duplicate"` 422 drops the stranded photo from the inbox instead of retrying it forever on every HomeScreen focus
- Improvement: tag write switched `POST` → `PUT` (replace, idempotent) on `/api/v3/tags` so a lost-response retry can't append duplicate tags / double-count XP for ordinary users; consumes `already_uploaded`/`tagged` from the upload response (skip tagging + clear inbox via `removeTaggedPhoto` when already tagged)
- Build: `jest.config.js` now transforms RTK/immer (ESM) so reducer/thunk tests can import slices; added tests for `isServerPhotoId`, `addTagsToPhoto` PUT + guard, and inbox cleanup (49 passing)
- Fix (v7.7.8): tag-write 422 "The selected photo id is invalid." — a `photo_id` that is a valid integer but has no live server row (stale local-counter id persisted from a pre-fix version, or a photo deleted server-side). `classifyError` now maps any `photo_id` validation 422 to a permanent, non-reportable `invalid-photo-id` (no Sentry spam), and `addTagsToPhoto.rejected` drops the photo from the inbox so the upload loop stops retrying every focus; transient errors still keep the photo for retry. New user string "This photo could not be tagged and was removed." added to all 8 languages. Tests added (54 passing)
- Build (v7.7.9): wire up Sentry symbolication for native App-Hang/crash stacks (forward-only, on release builds) — iOS uploads the app-binary dSYM plus the prebuilt React/Hermes release-artifact symbols (matching Debug IDs) via a guarded Xcode build phase; Android adds `io.sentry.android.gradle` (native `.so` symbols) + the RN `sentry.gradle` (JS/Hermes sourcemaps). Minify stays off so no ProGuard mapping upload is needed. Auth via `SENTRY_AUTH_TOKEN` env; `sentry.properties` (iOS+Android) hold org/project only, no token.
- Feature (v7.8.0): reworked the "Your Photos" inbox — dropped the 7-day window (was dead-ending users whose recent roll had no geotagged photos in the last week); now shows all geotagged photos newest-first with a location pin top-right, **6 as a preview** and a **"Load more photos"** button that reveals 50 more (camera-roll `LOAD` page size 20 → 50). Added a **"Select More"** button (inbox header, right of Delete) that opens the system gallery, reads GPS via EXIF, and imports geotagged picks straight into the tag flow (non-geotagged picks skipped with an alert). The HomeScreen dashboard is now a single virtualized `FlashList` (other sections in `ListHeaderComponent`, inbox photos as data); inbox logic extracted to a `useInbox` hook and `InboxSection` split into `InboxThumbnail`/`InboxControls`/`InboxEmpty`/`InboxFooter`. New `selectGeotaggedPhotos` selector + test; new i18n strings across all 8 languages.
- Polish (v7.8.1): "Your Photos" now shows **all** camera-roll photos — non-geotagged ones are greyed out and geotagged ones get a 📍 pin top-right (only geotagged are mappable). Fixed the grid: tiles are now flex-sized (`flex: 1` + `aspectRatio: 1`, matching the app's other FlashList grids) so they're equal and centered on any device instead of fixed-width and left-aligned with dead space; the grid is inset to a consistent 16px (full-width sections "bleed" back out so they keep their own padding). Selector renamed `selectGeotaggedPhotos` → `selectInboxPhotos`.
- Fix (v7.8.2): level titles (e.g. "Complete Noob") didn't update after a backend `/api/levels` change. Root cause — the endpoint returns an object keyed by XP threshold whose **value is the title string** (`{ "0": "Noob", "100": "Litter Picker", … }`), but `normalizeLevels` only handled arrays (fell back to a hardcoded ladder, ignoring the backend) and then briefly expected object *values* (producing "Level 1: Unknown"). Parser now reads the string-value shape, tolerates object values, and derives XP from the numeric key (still accepts arrays / `{ data }` / `{ levels }`); cache key bumped to `xp_levels_cache_v3` (drops stale fallback/"Unknown" caches) and the fallback titles updated to mirror the live ladder (Noob, Litter Picker, …, SuperIntelligent LitterMaster at 1,000,000). `normalizeLevels` exported + 4 tests added.
- Fix (v7.8.2): logout cache leak — `profile_stats_cache` (user-specific xp/position/totalImages) lived outside Redux and wasn't cleared on logout, so a freshly-created account could briefly show the previous user's stats. A `createListenerMiddleware` listener in `store/index.js` now clears `profile_stats_cache` + `xp_levels_cache_v3` on every `logout` (button, 401 auto-logout, account switch).
- Copy (v7.8.2): inbox empty-state string "No photos to map right now." → "Select Photos To Tag & Upload" (all 8 languages).
