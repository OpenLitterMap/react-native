## 7.7.0 — 2026-04-04

- Animated stat counters: HomeScreen numbers count up smoothly from old → new on pull-to-refresh (3s duration, Reanimated UI thread)
- Stats persistence: stats slice persisted via redux-persist so counters animate from last-known values on app restart
- Global Impact labels: "today/this week/this month" → "24h/7d/30d" to match rolling window semantics
- Backend field mapping: `new_*_today` → `new_*_last_24_hours` (backend serves both keys for backwards compatibility)
- App version check: re-enabled for production (was gated behind __DEV__ for testing, now restored)
- Fix: dismissed photos reappearing after app restart — `gallery.dismissedUris` now persisted via redux-persist
- Fix: gallery rehydration crash — gallery transform now returns full initial state shape (was only returning dismissedUris, causing `galleryImages` to be undefined)
