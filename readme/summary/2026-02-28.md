# Summary — 2026-02-28

## Files Created
- `CLAUDE.md` — Project guidance file for Claude Code instances
- `AUDIT.md` — Full codebase audit with 10 sections (file inventory, API calls, auth, state, navigation, deps, bugs, build config, feature state, assessment)
- `GPS_AUDIT.md` — GPS coordinate extraction audit documenting 0,0 bug and multi-layer fix
- `readme/MobileAuth.md` — Auth system documentation
- `readme/MobileUpload.md` — Upload flow documentation
- `readme/MobileTagging.md` — Litter tagging system documentation
- `readme/MobileTeams.md` — Teams feature documentation
- `readme/MobileSettings.md` — Settings feature documentation
- `readme/MobileGallery.md` — Gallery/photo selection documentation
- `readme/MobileLeaderboards.md` — Leaderboards documentation
- `readme/MobileGlobalStats.md` — Global stats documentation
- `readme/MobileMyUploads.md` — Upload history documentation
- `readme/MobileNavigation.md` — Navigation structure documentation
- `readme/MobilePermissions.md` — Permissions handling documentation

## GPS Fixes (Stages 1-5)

### Stage 1: Safety Net
- `images_reducer.js`: Changed `?? 0` to `?? null` on lat/lon (lines 220-221, 576-577)
- `isGeotagged.js`: Full rewrite — rejects null, undefined, and 0,0 coordinates

### Stage 2: Android ACCESS_MEDIA_LOCATION
- `cameraRollPermission.js`: `checkCameraRollPermission()` now checks AND requests ACCESS_MEDIA_LOCATION on Android 13+. Returns 'limited' if only READ_MEDIA_IMAGES granted.
- `checkAccessMediaLocation()`: Fixed to use `PERMISSIONS.ANDROID.ACCESS_MEDIA_LOCATION` constant

### Stage 3: Gallery Shows All Photos
- `gallery_reducer.js`: Renamed `geotaggedImages` → `galleryImages`, added `geotaggedCount`/`nonGeotaggedCount`, all photos loaded with `hasGps` boolean
- `GalleryScreen.js`: All photos displayed, non-GPS dimmed + not selectable, warning banner, empty state
- `AnimatedImage.js`: Non-geotagged: opacity 0.4 + red crossed-out location icon
- `AlbumList.js`: Updated to use `galleryImages`

### Stage 4: Debug Logging
- `gallery_reducer.js`: `__DEV__` logging with platform, counts, first 5 photos

### Stage 5: Pre-upload Validation
- `HomeScreen.js`: Filters non-geotagged before upload, Alert with counts

## Auth Sanctum Migration
- `auth_reducer.js`: Login URL `/api/login` → `/api/auth/token`, payload `{email, password}`
- `auth_reducer.js`: Register sends only `{email, password}`, username auto-generated server-side
- `SignupForm.js`: Stripped to email + password only (removed username field)
- `SigninForm.js`: Removed dead `errors?.username` check

## UX Polish
- Gallery empty state with icon + explanation + non-geotagged count
- Selection count badge next to "Next" button
- Fixed error display typo in HomeScreen thank-you modal
- Removed production console.logs (AddTags, images_reducer, SettingsScreen)
- Guarded GalleryPermissionScreen console.log with `__DEV__`
- Removed dead `loading` state variable from GalleryScreen
- Removed unused `Animated` import from GalleryScreen

## Bug Fixes (from initial audit)

### BUG-02: Auth Passport → Sanctum Migration
- Removed `CLIENT_ID`, `CLIENT_SECRET` exports and credential leaks from `actions/types.js`
- Updated login/register endpoints and payloads in `auth_reducer.js`

### BUG-03: Missing `return` before `rejectWithValue`
- `settings_reducer.js`: Added `return` in `saveSettings` and `saveSocialAccounts`

### BUG-04: Stats.tsx Wrong State Path
- Changed `state.user` to `state.auth.user`

### BUG-06: leaveTeam Fulfilled Handler Empty
- Added dispatch for cross-slice active team update, implemented team removal from `userTeams`

### BUG-07: Leaderboard Error Handler Crashes
- Safe-accessed `error.response?.data` with fallback message

### BUG-12: MyUploads Deprecated Field Reference
- Added `item.result_string || item.summary` fallback

### BUG-01: Permission Check Always Passes
- Fixed `result === 'granted' || 'limited'` to `result === 'granted' || result === 'limited'`

### BUG-05: SettingsComponent Broken Error Display
- Changed literal text to JSX expression

### BUG-08/09/10: Phantom Action Exports
- Removed non-existent reducer exports from auth, settings, team reducers

### BUG-11: TopTeamsScreen Fake Loading
- Removed artificial 3-second delay, loading derived from data state

### BUG-15: changeActiveTeam Fulfilled Payload Mismatch
- Fixed handler to work with numeric payload

### BUG-17: LitterBottomButtons Delete Button Disabled
- Wired `deleteButtonPressed` prop

## Dead Code Removal
- Deleted: `CameraScreen.js`, `MapScreen.js`, `web_reducer.js`, `GalleryRoutes.tsx`, `Colors.js`

## Auth Form Fixes and Login with Username

### Login with Email or Username
- `SigninForm.js`: Login field accepts email or username (was email-only)
- `auth_reducer.js` `userLogin`: Detects email (contains `@`) vs username, sends `{email, password}` or `{username, password}` accordingly
- `auth.json`: Added `email-or-username` and `enter-email-or-username` translation keys

### Auth Form Bug Fixes
- **SigninForm.js**: Fixed `.toLowerCase()` on every keystroke preventing capital letters in usernames — normalization moved to submit time in the thunk
- **SigninForm.js**: Removed `multiline` prop that broke keyboard return-to-next-field on iOS
- **SigninForm.js**: Replaced inline `<Text>` error display with `StatusMessage` component for consistency
- **SigninForm.js**: Removed hardcoded "Create Account" fallback text from login button
- **SigninForm.js**: Validation schema now applies in all modes (was skipped in dev via `__DEV__` check)
- **SignupForm.js**: Removed `multiline` prop from email field
- **SignupForm.js**: Standardized password `onChangeText` to use `setFieldValue` (was `handleChange`)
- **ForgotPasswordForm.js**: Fixed button showing "Create Account" instead of "Forgot password?"
- **ForgotPasswordForm.js**: Fixed nonexistent `auth.email-error` translation key — now passes Yup errors properly
- **ForgotPasswordForm.js**: Fixed `toLocaleLowerCase()` → `toLowerCase()` consistency
- **ForgotPasswordForm.js**: Fixed icon `"mail"` → `"mail-outline"` for consistency
- **ForgotPasswordForm.js**: Removed unused styles (`textfieldIcon`, `buttonDisabled` — latter now used)
- **CustomTextInput.tsx**: Fixed `errorMessage` container `height: 0` preventing errors from rendering
- **CustomTextInput.tsx**: Enabled error border styling (was commented out)
- **CustomTextInput.tsx**: Enabled error text color styling (was commented out)
- **CustomTextInput.tsx**: Cleaned up commented-out props and unused interface fields

## Dependency Upgrades
- `@react-native-community/masked-view` → `@react-native-masked-view/masked-view`
- `react-native-swiper` → `react-native-pager-view`
- `moment` → `dayjs`
